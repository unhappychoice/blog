machine:
  ruby:
    version: 2.2.2
  pre:
    - "git config --global user.name 'Circle CI'"
    - "git config --global user.email 'unhappychoice@gmail.com'"
compile:
  override:
    - bundle exec middleman build
test:
  override:
    - echo "No test"
deployment:
  master:
    branch: master
    commands:
      - "scripts/deploy.sh"
  generate:
    branch: github-article-generator
    commands:
      - "bundle exec ruby scripts/github_event_article_generator.rb"
